# The Expo SDK 57 dependency tree resolves only with legacy peer deps (e.g. the
# @react-native/jest-preset peer on react-native, and other SDK-57 peers). The
# lockfile was generated this way, so every install — local `npm install`/`npm ci`
# and CI — must use it or npm ERESOLVE-fails.
legacy-peer-deps=true
